# 🏗️ System Architecture - Admin Authentication

## 📊 Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser)                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────┐│
│  │ /admin/login │  │    /admin    │  │ /admin/manage-admins   ││
│  │  AdminLogin  │  │  Dashboard   │  │   AdminManagement      ││
│  └──────┬───────┘  └──────┬───────┘  └──────┬─────────────────┘│
│         │                 │                  │                   │
└─────────┼─────────────────┼──────────────────┼───────────────────┘
          │                 │                  │
          │   HTTP/HTTPS    │                  │
          ▼                 ▼                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      MIDDLEWARE (Next.js)                        │
├─────────────────────────────────────────────────────────────────┤
│  middleware.ts                                                   │
│  • Validates session cookies                                     │
│  • Checks authentication for /admin/* routes                     │
│  • Redirects unauthorized users to /admin/login                  │
│  • Validates user isActive status                                │
└─────────────────┬───────────────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                    API ROUTES (Next.js)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  AUTHENTICATION                         ADMIN CRUD               │
│  ┌────────────────────────┐            ┌─────────────────────┐  │
│  │ POST /api/admin/auth/  │            │ GET /api/admin      │  │
│  │      login             │            │ POST /api/admin     │  │
│  │      verify-otp        │            │ GET /api/admin/[id] │  │
│  │      logout            │            │ PUT /api/admin/[id] │  │
│  │ GET  session           │            │ DEL /api/admin/[id] │  │
│  └───────────┬────────────┘            └──────────┬──────────┘  │
│              │                                    │              │
└──────────────┼────────────────────────────────────┼──────────────┘
               │                                    │
               ▼                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                      LIBRARY LAYER                               │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌──────────────────┐  ┌────────────────┐ │
│  │   lib/auth.ts   │  │  lib/email.ts    │  │  lib/utils.ts  │ │
│  │                 │  │                  │  │                │ │
│  │ • Lucia setup   │  │ • Nodemailer cfg │  │ • Validation   │ │
│  │ • Custom adapter│  │ • OTP templates  │  │ • Sanitization │ │
│  │ • Session mgmt  │  │ • Email sending  │  │ • Helpers      │ │
│  │ • Validation    │  │ • Gmail SMTP     │  │ • Role checks  │ │
│  └────────┬────────┘  └─────────┬────────┘  └────────────────┘ │
│           │                     │                                │
└───────────┼─────────────────────┼────────────────────────────────┘
            │                     │
            ▼                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                             │
├─────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────┐    ┌──────────────────────────────┐  │
│  │   Gmail SMTP Server   │    │    PostgreSQL Database       │  │
│  │   (smtp.gmail.com)    │    │                              │  │
│  │                       │    │  ┌────────────────────────┐  │  │
│  │  • Sends OTP emails   │    │  │ Admin Table            │  │  │
│  │  • Uses App Password  │    │  │ • id, email, password  │  │  │
│  │  • TLS encryption     │    │  │ • name, role, isActive │  │  │
│  │                       │    │  └────────────────────────┘  │  │
│  └───────────────────────┘    │  ┌────────────────────────┐  │  │
│                                │  │ Session Table          │  │  │
│                                │  │ • id, userId, expires  │  │  │
│                                │  └────────────────────────┘  │  │
│                                │  ┌────────────────────────┐  │  │
│                                │  │ OTP Table              │  │  │
│                                │  │ • id, code, expires    │  │  │
│                                │  │ • adminId, verified    │  │  │
│                                │  └────────────────────────┘  │  │
│                                └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Authentication Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Navigate to /admin/login
     ▼
┌──────────────────┐
│  AdminLogin Page │
└────┬─────────────┘
     │ 2. Enter email & password
     ▼
┌────────────────────────┐
│ POST /api/admin/auth/  │
│      login             │
└────┬───────────────────┘
     │ 3. Verify credentials
     ▼
┌────────────────────┐
│  Database Query    │ ◄──── Bcrypt compare password
│  Find admin by     │
│  email             │
└────┬───────────────┘
     │ 4. If valid
     ▼
┌────────────────────┐
│  Generate OTP      │ ◄──── 6-digit random code
│  Save to DB        │       Expires in 10 minutes
└────┬───────────────┘
     │ 5. Send OTP
     ▼
┌────────────────────┐
│  Nodemailer        │ ◄──── Beautiful HTML email
│  Send email via    │       Contains OTP code
│  Gmail SMTP        │
└────┬───────────────┘
     │ 6. User receives email
     ▼
┌────────────────────┐
│  User enters OTP   │
└────┬───────────────┘
     │ 7. Submit OTP
     ▼
┌────────────────────────┐
│ POST /api/admin/auth/  │
│      verify-otp        │
└────┬───────────────────┘
     │ 8. Validate OTP
     ▼
┌────────────────────┐
│  Database Query    │ ◄──── Check code & expiry
│  Find OTP record   │       Mark as verified
└────┬───────────────┘
     │ 9. If valid
     ▼
┌────────────────────┐
│  Lucia             │ ◄──── Create session
│  Create session    │       30-day expiry
│  Set cookie        │       HTTP-only secure
└────┬───────────────┘
     │ 10. Redirect to dashboard
     ▼
┌────────────────────┐
│  /admin            │ ◄──── Session validated
│  Dashboard         │       User authenticated
└────────────────────┘
```

## 🛡️ Security Layers

```
┌───────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                         │
├───────────────────────────────────────────────────────────┤
│                                                            │
│  Layer 1: Transport Security                               │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • HTTPS in production                               │ │
│  │  • Encrypted data transmission                       │ │
│  │  • Secure headers                                    │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  Layer 2: Middleware Protection                            │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • Route-level authentication                        │ │
│  │  • Session validation                                │ │
│  │  • Role-based access control                         │ │
│  │  • Active account check                              │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  Layer 3: Authentication                                   │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • Email + Password (1st factor)                     │ │
│  │  • OTP code (2nd factor)                             │ │
│  │  • Session management                                │ │
│  │  • Automatic session refresh                         │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  Layer 4: Password Security                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • Bcrypt hashing (10 rounds)                        │ │
│  │  • No plain text storage                             │ │
│  │  • Strong password requirements                      │ │
│  │  • Secure password reset                             │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  Layer 5: OTP Security                                     │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • Time-limited (10 minutes)                         │ │
│  │  • Single-use (verified flag)                        │ │
│  │  • 6-digit random code                               │ │
│  │  • Email delivery only                               │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  Layer 6: Session Security                                 │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • HTTP-only cookies                                 │ │
│  │  • Secure flag in production                         │ │
│  │  • SameSite protection                               │ │
│  │  • 30-day expiration                                 │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  Layer 7: Database Security                                │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • Parameterized queries (Prisma)                    │ │
│  │  • SQL injection prevention                          │ │
│  │  • Input sanitization                                │ │
│  │  • Cascading deletes                                 │ │
│  └──────────────────────────────────────────────────────┘ │
│                           ▼                                │
│  Layer 8: Application Security                             │
│  ┌──────────────────────────────────────────────────────┐ │
│  │  • Role-based permissions                            │ │
│  │  • Account activation/deactivation                   │ │
│  │  • Self-deletion prevention                          │ │
│  │  • Audit logging                                     │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
└───────────────────────────────────────────────────────────┘
```

## 📦 Data Flow - Create Admin

```
┌─────────────┐
│  Admin User │
│  (Role: admin)
└──────┬──────┘
       │ 1. Click "Add Admin"
       ▼
┌────────────────────┐
│  AdminManagement   │
│  Component         │
└──────┬─────────────┘
       │ 2. Fill form (email, password, name, role)
       ▼
┌────────────────────┐
│  Form Validation   │ ◄──── Client-side validation
│  • Email format    │       • Required fields
│  • Password rules  │       • Role selection
└──────┬─────────────┘
       │ 3. Submit form
       ▼
┌────────────────────┐
│  POST /api/admin   │
└──────┬─────────────┘
       │ 4. Validate session
       ▼
┌────────────────────┐
│  Middleware Check  │ ◄──── Verify authenticated
│  validateRequest() │       Verify admin role
└──────┬─────────────┘
       │ 5. If authorized
       ▼
┌────────────────────┐
│  API Route Handler │ ◄──── Server-side validation
│  • Check email     │       • Password strength
│  • Check duplicates│       • Role validation
└──────┬─────────────┘
       │ 6. Hash password
       ▼
┌────────────────────┐
│  Bcrypt.hash()     │ ◄──── 10 rounds
│  Generate hash     │
└──────┬─────────────┘
       │ 7. Create record
       ▼
┌────────────────────┐
│  Prisma Create     │ ◄──── Insert into database
│  admin.create()    │       Return new admin
└──────┬─────────────┘
       │ 8. Success response
       ▼
┌────────────────────┐
│  Component Update  │ ◄──── Close modal
│  • Refresh list    │       Show success message
│  • Show new admin  │       Clear form
└────────────────────┘
```

## 🔍 Component Hierarchy

```
App
├── middleware.ts (Global protection)
│
├── /admin/login
│   └── AdminLogin.tsx
│       ├── Email + Password form
│       └── OTP verification form
│
├── /admin
│   └── AdminDashboard.tsx
│       └── (Existing dashboard content)
│
└── /admin/manage-admins
    └── ManageAdminsPage.tsx
        └── AdminManagement.tsx
            ├── Admin list table
            ├── Create admin modal
            ├── Edit admin modal
            └── Delete confirmation
```

## 🔐 Role Permissions Matrix

```
┌──────────────────────┬────────────────┬────────────────┐
│      Action          │  Admin Role    │ Manager Role   │
├──────────────────────┼────────────────┼────────────────┤
│ View Admin List      │       ✅       │       ❌       │
│ Create Admin         │       ✅       │       ❌       │
│ Edit Any Admin       │       ✅       │       ❌       │
│ Edit Own Profile     │       ✅       │       ✅       │
│ Delete Admin         │       ✅       │       ❌       │
│ Change Admin Role    │       ✅       │       ❌       │
│ Activate/Deactivate  │       ✅       │       ❌       │
│ View Dashboard       │       ✅       │       ✅       │
│ Manage Contestants   │       ✅       │       ✅       │
│ Access /admin/*      │       ✅       │       ✅       │
│ Access manage-admins │       ✅       │       ❌       │
└──────────────────────┴────────────────┴────────────────┘
```

## 📧 Email Flow

```
┌──────────────────┐
│  OTP Requested   │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────┐
│  lib/email.ts            │
│  generateOTPEmail()      │
└────────┬─────────────────┘
         │ HTML template with:
         │ • Branding
         │ • OTP code (large, styled)
         │ • Expiry warning
         │ • Security notice
         ▼
┌──────────────────────────┐
│  nodemailer.sendMail()   │
│  • From: Unity Vote Admin│
│  • To: Admin email       │
│  • Subject: Login Code   │
│  • HTML: Beautiful email │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│  Gmail SMTP              │
│  smtp.gmail.com:587      │
│  • TLS encryption        │
│  • App password auth     │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────┐
│  Admin's Email Inbox     │
│  • Formatted email       │
│  • 6-digit code          │
│  • Valid for 10 min      │
└──────────────────────────┘
```

---

**This architecture provides:**
- ✅ Robust security with multiple layers
- ✅ Clear separation of concerns
- ✅ Scalable design
- ✅ Maintainable codebase
- ✅ Industry-standard practices
